//add movies and all info for the movies including trailer link
import React from 'react';

const AddMovie = () => {
    return (
        <div>
            Test for component page
        </div>
    );
};

export default AddMovie;