import React from 'react';
import './home.css';

const Home = () => {
    return (
        <div className="homepg">
            Main Page of the website, will include the carousel and movie listings. <br/>

            We can have React fetch the movies and display them as a list (almost like a blog)

        </div>
    );
};

export default Home;